import { innerject } from './innerject';
import { innerjectFunc } from './innerjectFunc';
import { combinedRegistryResolve } from './combinedRegistryResolve';
import Registry, { defaultRegistry } from './registry';

export {
    innerject,
    innerjectFunc,
    combinedRegistryResolve,
    Registry,
    defaultRegistry,
}